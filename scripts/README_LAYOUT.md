These scripts currently assume a monorepo-style layout (ROOT = parents[1]).
For a fully standalone package, keep `scripts/` under this export root.
Ingest and one-pager mkdir `docs/final/artifacts/` + `reports/human_paste/` on first run.
